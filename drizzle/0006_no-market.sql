-- The contract board is gone. Nothing was ever posted on it in production, so
-- these two tables go with it rather than sitting empty forever.
DROP TABLE IF EXISTS `bid`;
DROP TABLE IF EXISTS `contract`;
